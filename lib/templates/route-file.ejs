"use strict";
const Joi = require("joi");
module.exports = (server) => {
  const { <%=ModelName%> } = server.models();
  const responseSchema = <%=ModelName%>.responseSchema.label('<%=ModelName%>');
  return [
    <% for (const endpoint of endpoints) {%>
      <%-include(templatepath, {endpoint: endpoint})-%>
    <%}-%>
  ]
};